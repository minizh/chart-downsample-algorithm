### Echarts 内置配置

line: smapling (100w 886ms 225MB)

bar: sampling + large (100w 1041ms 232MB)

scatter: large + progressive (100w 首屏5s 1fps)

### 降采样算法

Line Chart: 
算法: LTTB 标准版
原始数据:
1M
采样后:
5K
压缩比:
200:1
采样耗时:
876.10ms
渲染耗时:
108.40ms
数据内存:
0.2MB
原始数据内存:
30.5MB
内存变化:
93MB ~ 235MB -> +142MB
渲染完成后:
110MB 60fps

Bar Chart: 
采样算法:
等宽聚合（求和）
原始数据:
1.0M
采样后:
5.0K
压缩比:
200.0:1
采样耗时:
1011.80ms
渲染耗时:
65.50ms
数据内存:
0.2MB
原始数据内存:
30.5MB
内存变化:
93MB ~ 257MB -> +165MB
渲染完成后:
110MB 60fps

Box Chart: 
采样算法:
五数概括
原始数据:
983.0K
采样后:
216.6K
压缩比:
4.5:1
采样耗时:
1061.10ms
渲染耗时:
1508.20ms
数据内存:
6.6MB
原始数据内存:
30.0MB
内存变化:
345MB ~ 445MB -> +100MB
渲染完成后:
345MB 60fps
一共 25205 组 box，其中 2821 组 box 有离群点, 共 10501 个离群点

Scatter Chart:
算法: KDE 加权
原始数据:
1M
采样后:
5K
压缩比:
200:1
采样耗时:
1910.20ms
渲染耗时:
24.70ms
数据内存:
0.2MB
原始数据内存:
30.5MB
内存变化:
118MB ~ 325MB -> +210MB
渲染完成后:
135MB 60fps

### 待确认：

1. 折线图，LLTB 算法要求有序数值（时间戳或连续数值），不适用于类目轴，类目轴场景是否存在大数据量需求？如果有，采用 dataZoom 仅支持查看局部数据方案能否接受？

    类目轴折线图的采样优化框架：

    1.1 整体趋势保持

        手动建立字符串与数值的映射关系，再使用 LLTB 算法降采样，x label 使用 formatter 转换为字符串

    1.2 全部极值点保留 `TODO`

    1.3 特定数据点精确值 `TODO`：
        
        用户标注点、业务关键点、异常检测点等

2. 所有采样算法均会导致完整数据丢失，用户能否接受，是否有查看原始数据的场景？

    2.1 采样后，用户依旧可以查看原始数据：
        
        自定义 Tooltip - 通过像素坐标到数据坐标的精确转换，结合二分查找算法，实现毫秒级原始数据定位

### 遗留问题

1. 数据采样后可能会导致 echarts 功能部分异常，如 tooltip、legend、dataZoom、visualMap 等，需要确认采样后这些功能是否可用，如果不可用，需要考虑采样后如何实现这些功能。